#include <stdio.h>
#include <stdlib.h>
#include <gmpxx.h>

void add(int i, int j){

}
int main(){
    char **Answers = calloc(11, sizeof(char*));
    for(i = 0 ; i < 11 ; i++)
    {
        Answers[i] = calloc(1, sizeof(char)*10);
    }
    return 0;
}